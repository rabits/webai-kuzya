fastapi>=0.111
uvicorn[standard]>=0.29
pydantic>=2.0
numpy>=1.26,<2
soundfile>=0.12
silero-stress>=0.1
f5-tts>=1.1.0
# torch/torchaudio are installed from the compose TORCH_INDEX_URL in the Dockerfile.
# torchcodec is CUDA-only (libnvrtc); F5-TTS reads wavs via soundfile instead.
